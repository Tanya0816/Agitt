For now the the deep audit in pass 2  searches file present in the given source or directory, it does not look for external sources if the file or the function which needs to be audited is from external source and is inherited in the contract . 
....
Need to work in improving this working ....


I want Agitt to focus on how the child contract inherits a contract and how the child contract uses it . 

Pass2 only gets extracted functions from pass1 triage so it has no idea about the inheritance if used in the contract.


